# This playbook will do some initial configuration of Vagrant
# boxes by setting test files e.g. /etc/hosts such that they can be used for
# testing
---
  - hosts: test
    sudo: true
    roles:
      - vagrant-test-files
   
