---
  #- hosts: all
  - hosts: localhost
    connection: local
    sudo: true
    roles: 
      - IHTSDO.roletest
     # - IHTSDO.debug 
     #- { role: IHTSDO_roletest }
     #- { role: IHTSDO.roletest }
