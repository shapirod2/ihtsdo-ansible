---
- name: Populate and install init script
  template: src=templates/init.j2 dest=/etc/init.d/crowd owner=root group=root mode=755

# Register service
- name: Register Service
  service: name=crowd  enabled=yes state=started
