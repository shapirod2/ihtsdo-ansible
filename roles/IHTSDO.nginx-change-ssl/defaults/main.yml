---
#nginx_conf_file: "*"
nginx_conf_dir: "/etc/nginx/conf.d"
nginx_sites_dir: "/etc/nginx/sites-enabled"

#find_command: "find -L {{ nginx_conf_dir }} -type f -name '{{ nginx_conf_file }}'"
find_command: "find -L {{ nginx_conf_dir }} -type f"

find_command_sites: "find -L {{ nginx_sites_dir }} -type f"

